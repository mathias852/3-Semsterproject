package org.example.BeerMachine;

import org.example.BeerMachine.data.models.Type;
import org.example.BeerMachine.web.TypeController;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

import java.util.List;

@SpringBootApplication
public class BeerMachineApplication {

	public static void main(String[] args) {
		SpringApplication.run(BeerMachineApplication.class, args);
	}
}
